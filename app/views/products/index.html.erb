<div id="agreementModal" class="modal hide fade" tabindex="-1" role="dialog">
  <div class="modal-header">
    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
    <h3 id="myModalLabel">Production Agreement</h3>
  </div>
  <div class="modal-body">
  </div>
  <div class="modal-footer">
    <button class="btn" data-dismiss="modal" aria-hidden="true">Discuss Agreement</button>
    <button class="btn btn-success">Accept Agreement</button>
  </div>
</div>

<div class="row">
  <div class="span12">
    <div class="pull-right actions-bar">
      <%= link_to 'Propose New', new_agreement_path, class: "btn btn-primary" %>
      <div class="btn-group ">
	<%= link_to raw('My Agreements'), "#", class: "btn btn-success" %>
	<%= link_to raw('<span class="caret"></span>'), "#", class: "btn btn-success dropdown-toggle", data: {toggle: "dropdown"} %>
	<ul class="dropdown-menu">
	  <li><%= link_to 'Proposed', "#" %></li>
	  <li><%= link_to 'Active', "#" %></li>
	  <li><%= link_to 'Past', "#" %></li>
	</ul>
      </div>
      <%= link_to raw('<span class="icon-question-sign"></span>'), "#", class: "btn" %>
    </div>
    <h1><img class="big-icon" src="/assets/icon-caln.png" alt="Calendar Icon" />  <%= params['cat_id'].blank? ? "Production Agreements" : Category.where(id: params["cat_id"].to_i).first.name %> <%= raw("<small>(" + @products.count.to_s + ")</small>") if current_user.admin? %></h1>
  </div>
</div>

<div class="row">
  <div class="span12">
    <%= render "product_table" %>
  </div>
</div>
