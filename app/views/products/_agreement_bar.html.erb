<a class="bar <%= a.bar_status(current_user.id) %>" style="<%= a.bar_margins %>" href="agreements/<%= a.id %>/modal" remote="true" data-target="#agreementModal" data-toggle="modal" data-placement="top" data-content="<strong><%= a.name %></strong><br /><strong>Price:</strong> <%= number_to_currency(a.price) %>/<%= UNIT_TYPE[a.selling_unit] %><br /><strong>Amount:</strong> <%= pluralize(a.quantity, UNIT_TYPE[a.selling_unit]) %> | <%= a.frequency %><br /><strong>Duration:</strong> <%= a.duration %><br /><br /><small>Click bar for contract details...</small>" title="" data-html="true" data-original-title="<%= current_user.producer? ? a.buyer.buyer_profile.name : a.producer.producer_profile.name %>"></a>
