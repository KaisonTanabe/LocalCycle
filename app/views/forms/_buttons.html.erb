<% if current_user.educator? or form.user == current_user %>

  <div class="btn-toolbar visible-desktop">
    <div class="btn-group">
      <%= link_to raw('<i class="icon-file icon-white"></i> Edit Form &nbsp;'), edit_form_path(form), title: "Update info in this form", class: "btn btn-small btn-inverse" %>
    </div>
    <div class="btn-group">
      <%= (link_to raw('<i class="icon-trash"></i>'), form_path(form, full_params.to_hash), data: {confirm: "Are you sure you want to permanently delete this form?"}, title: "Delete this form from the system", method: :delete, class: "btn btn-small") %>
    </div>
  </div>
  <div class="btn-group hidden-desktop">
    <div class="btn-group">
      <%= link_to raw('<i class="icon-file icon-white"></i> Edit &nbsp;'), edit_form_path(form), title: "Update info in this form", class: "btn btn-small btn-inverse" %>
    </div>
    <div class="btn-group">
      <%= (link_to raw('<i class="icon-trash"></i>'), form_path(form, full_params.to_hash), data: {confirm: "Are you sure you want to permanently delete this form?"}, title: "Delete this form from the system", method: :delete, class: "btn btn-small") %>
    </div>
  </div>

<% else %>

  <div class="btn-toolbar visible-desktop">
    <div class="btn-group">
      <%= link_to raw('<i class="icon-file icon-white"></i> View Form'), edit_form_path(form), title: "Review this form", class: "btn btn-small btn-inverse" %>
    </div>
    <div class="btn-group">
      <%= (link_to raw('<i class="icon-trash"></i>'), form_path(form, full_params.to_hash), data: {confirm: "Are you sure you want to permanently delete this form?"}, title: "Delete this form from the system", method: :delete, class: "btn btn-small") unless current_user.cm?%>
      <!--a class="btn btn-small dropdown-toggle" data-toggle="dropdown" href="#">
	<i class="icon-bell"></i><span class="caret"></span>
      </a>
      <ul class="dropdown-menu">
	<li><%= link_to raw('<i class="icon-user"></i> Prompt'), prompt_user_path(form.user), title: "Send email to user prompting them of this form's status." %></li>
      </ul-->
    </div>
  </div>

  <div class="btn-group hidden-desktop">
    <div class="btn-group">
      <%= link_to raw('<i class="icon-file icon-white"></i> Review'), edit_form_path(form), title: "Review this form", class: "btn btn-small btn-primary" %>
      <%= (link_to raw('<i class="icon-trash"></i>'), form_path(form, full_params.to_hash), data: {confirm: "Are you sure you want to permanently delete this form?"}, title: "Delete this form from the system", method: :delete, class: "btn btn-small") unless current_user.cm? %>
      <!--a class="btn btn-small dropdown-toggle" data-toggle="dropdown" href="#">
	<i class="icon-bell"></i><span class="caret"></span>
      </a>
      <ul class="dropdown-menu">
	<li><%= link_to raw('<i class="icon-user"></i> Prompt'), prompt_user_path(form.user), title: "Send email to user prompting them of this form's status." %></li>
      </ul-->
    </div>
  </div>
  
<% end %>
