<%= form_for(@market, html: { multipart: true, class: "form-horizontal" }) do |f| %>

  <legend></legend>
  <%= render partial: 'partials/errors', locals: {obj: @market} %>

  <%= render partial: 'form_fields', locals: {f: f} %>

  <%= render partial: 'partials/submit', locals: {f: f, text: 'Market' } %>

<% end %>
