<%= render partial: "users/modals/producer", locals: {user: user} if user.producer? %>
<%= render partial: "users/modals/buyer", locals: {user: user} if user.buyer? %>
<%= render partial: "users/modals/market_manager", locals: {user: user} if user.market_manager? %>

<br class="clear" />
